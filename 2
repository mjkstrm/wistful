mod token;
// Return a new tokenizer object.
pub struct Tokenizer {
    pub text: String
}
// Method implementations for tokenizer.
impl Tokenizer {
    // Instance methods
    pub fn get_token_stream(&self) -> u8 {
        let tokens: Vec<Token> = Vec::new();

        let mut test = "Testing";
        //let token = parser::token::Token { token::TokenType::Number, test, 7 };
        println!("Token was made: {0}", token::TokenType::Number);
    } 
    //Static methods 
}
